1. RedMango:
Built with Next.js and Asp.core, Redux;
Simple e-commerce store with Payment Gateway (WayForPay) and Cloudinary Media Cloud integration, with fully-featured basket 

2. Task App (Built using microservices architecture):
Built with Next.js and Asp.core, RabbitMQ, SignalR;
Backend consists of 3 microservices:
NotificationService (serves SignalR interactions for dynamic task CRUD and other notifications);
UserService (Profile + Authentication);
TaskService (CRUD operations with a task);
